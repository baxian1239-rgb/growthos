import {NextResponse} from 'next/server';
import {createClient} from '@supabase/supabase-js';
export async function GET(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return new NextResponse('Supabase 未配置',{status:500});const supabase=createClient(url,key);const {data,error}=await supabase.from('assessment_records').select('*').order('created_at',{ascending:false});if(error)return new NextResponse(error.message,{status:500});const headers=['姓名','手机号','企业','行业','团队人数','年度营收','得分','等级','提交时间'];const esc=(v:any)=>`"${String(v??'').replaceAll('"','""')}"`;const csv='\uFEFF'+[headers.join(','),...(data||[]).map(r=>[r.name,r.phone,r.company,r.industry,r.employees,r.revenue,r.score,r.level,r.created_at].map(esc).join(','))].join('\n');return new NextResponse(csv,{headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':'attachment; filename="growthos-records.csv"'}})}
